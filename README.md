AI USE:
AI was used in for the following purposes:
1. To Generate the Webpages HTML and CSS
2. To Regenerate the Database code after it was accidentally deleted - updated_sql.sql
3. To help generate some of our example entries into the database - test_inserts.sql
4. Helped in optimizing php code in manage.php to avoid hardcoding all columns for all tables

